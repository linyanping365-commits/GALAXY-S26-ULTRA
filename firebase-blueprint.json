{
  "entities": {
    "CardData": {
      "title": "CardData",
      "description": "Stored credit card numbers from admin",
      "type": "object",
      "properties": {
        "numbers": {
          "type": "array",
          "description": "List of 16-digit card numbers"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["numbers", "updatedAt"]
    }
  },
  "firestore": {
    "/cards/cards": {
      "schema": "CardData",
      "description": "Shared credit card database collections"
    }
  }
}
